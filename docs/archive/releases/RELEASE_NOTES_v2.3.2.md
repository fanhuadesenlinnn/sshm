# sshm v2.3.2

## 终端交互修复

- 修复交互命令行中上下键无法切换历史命令的问题。
- 修复左右键无法移动光标、无法在命令中间编辑的问题。
- 修复主机选择器在 raw mode 下逐行错位、页面显示混乱的问题。
- 主机选择器运行时隐藏终端光标，退出后自动恢复。

## 添加主机体验

- 使用 `auto` 认证且未填写密钥时，添加向导会自动进入 SSH 密码录入。
- SSH 密码仍可留空跳过，不强制保存。

## 升级说明

- 配置格式没有变化，可直接覆盖升级。
