# sshmd v6.2.1

v6.2.1 聚焦文档：初始化生成的 README 升级为完整使用指南，并清理文档中的历史迁移表述。主配置 schema 与 Deploy schema 均无变化。

## 初始化文档

- `sshmd init` 生成的 `README.md` 从一页速查升级为完整使用指南（约 120 行、10 章）：目录结构、主机管理、认证与凭据、连接与批量执行、文件传输、端口转发、Deploy 编排（模块与任务特性速查 + 最小 play 示例）、日志与诊断、安全边界、快速上手。
- 文档只描述当前情况，不再包含版本迁移历史。

## 文档清理

- README/CONTEXT/PRODUCT_DESIGN/ROADMAP 移除“v6 破坏性变更”“升级注意（v6.1.0）”“Deploy v2 已移除”等历史表述，保留当前事实（schema 版本、数据目录、命令约定等）。

## 质量

- 更新初始化 README 的回归断言，全量测试通过。
