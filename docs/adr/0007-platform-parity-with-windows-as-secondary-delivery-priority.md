# Platform parity with Windows as secondary delivery priority

Linux, macOS, and Windows have equal final product capability commitments and user-visible semantics, but Windows may receive new capabilities after Linux and macOS. Any Windows delay must be explicit, temporary, and tracked; platform-specific implementation differences are acceptable, while permanent feature exclusions and silent failures are not.
