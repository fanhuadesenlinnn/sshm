# Secure by default with explicit overrides

sshm defaults to strict host identity verification, encrypted credential storage, and confirmation before dangerous or batch operations. Convenience options may reduce repeated interaction only when the user explicitly chooses them and the risk is clearly communicated; no feature or fallback path may silently weaken a security control.
