# SSH Host Management

sshm 帮助个人使用者管理日常 SSH 主机、认证凭据和远程操作，同时保持轻量和本地优先。

## Language

**核心用户**:
需要管理约 5～100 台服务器的个人开发者或个人运维使用者。
_Avoid_: 团队，企业管理员，大型组织管理员

**主机**:
核心用户需要通过 SSH 连接和操作的一台远程服务器。
_Avoid_: 资产，节点

**主机草录**:
尚未验证远端可达性或认证，但已作为本地配置保存的一台主机。
_Avoid_: 无效主机，失败主机

**托管密钥**:
由 sshm 的主密码保护，并可绑定到一个或多个主机的 SSH 私钥。
_Avoid_: 文件密钥，普通私钥

**主密码会话**:
当前 sshm 进程成功解锁密码库后，到主动锁定或进程退出为止的凭据使用时间段。
_Avoid_: 登录会话，后台缓存

**临时密码**:
仅用于当前一次连接尝试、未经明确批准不会保存的 SSH 密码。
_Avoid_: 保存密码，主密码

**目标集合**:
一次远程操作选中的一个或多个主机。
_Avoid_: 资产组，持久集合

**标签**:
核心用户为主机添加的可多选分类，用于查找和批量选择。
_Avoid_: 分组，目录

**收藏**:
核心用户显式标记、用于快速访问的高频主机状态。
_Avoid_: 标签

**最近使用**:
按照成功连接时间帮助核心用户快速返回主机的个人状态。
_Avoid_: 收藏

**连接旅程**:
核心用户从查找主机到建立可靠 SSH 会话的完整过程。
_Avoid_: 登录流程

**批量操作**:
核心用户在场时，经过范围确认后对一个目标集合执行的一次性远程动作。
_Avoid_: 群控，任务编排，持续部署

**主机信任**:
核心用户确认远程主机身份后，sshm 记录并在后续 SSH 操作中持续验证该身份。
_Avoid_: 自动接受，忽略指纹

**显式批准**:
核心用户在看到影响范围或风险后，对单次操作作出的明确授权。
_Avoid_: 默认同意，静默跳过

**原生能力**:
由 sshm 自身跨平台实现、定义并保证行为一致的 SSH 能力。
_Avoid_: 系统 SSH 透传，外部命令回退

**传输加速**:
在不改变安全语义和任务结果的前提下，使用可选 rsync 提升文件传输效率。
_Avoid_: 必需依赖，安全降级

**传输结果**:
一次文件或目录传输在一台主机上的源、目标、方式和完成状态。
_Avoid_: 仅进度，传输汇总

**覆盖批准**:
核心用户明确允许一次传输替换执行前已展示目标中的现有内容。
_Avoid_: 默认覆盖，隐式覆盖

**执行确认**:
核心用户在看到动作内容和具体目标后，对一次远程操作作出的明确批准。
_Avoid_: 主机信任，永久批准

**跳板机**:
核心用户已配置并信任、用于建立到目标主机连接的一台中间主机。
_Avoid_: 代理服务器，堡垒机平台

**本地端口转发**:
通过一台主机的 SSH 连接，将本地监听端口转发到指定远端地址的临时连接能力。
_Avoid_: 远程转发，动态代理

**私钥导入**:
读取普通私钥文件并将其转换为受主密码保护的托管密钥。
_Avoid_: 长期引用文件私钥

**数据目录**:
包含 sshm 所有个人配置、状态和加密凭据，可由核心用户整体复制或同步的目录。
_Avoid_: 备份包，云空间

**工作台**:
核心用户无参数启动 sshm 后，用于发现主机和完成引导式操作的轻量交互入口。
_Avoid_: 全屏 TUI，命令帮助墙

**别名直连**:
核心用户使用已知主机别名或 ID 建立 SSH 会话的最快入口。
_Avoid_: 引导式连接

**逐主机结果**:
批量操作完成后，为目标集合中的每台主机记录成功状态或具体失败信息的结果。
_Avoid_: 仅汇总，失败数量

**详细结果模式**:
按主机依次展示远程命令完整输出，并在最后汇总成功与失败主机的批量输出方式。
_Avoid_: 交错输出，实时混流

**简洁状态模式**:
隐藏成功主机的远程命令输出，仅展示逐主机状态和完整失败信息的批量输出方式。
_Avoid_: 静默执行，仅总数

**操作日志**:
保存在数据目录中、按批量操作和主机记录完整远程输出与诊断信息的个人日志。
_Avoid_: 审计日志，团队日志

**失败阶段**:
逐主机结果中用于指出操作在哪个明确步骤失败的分类。
_Avoid_: 通用失败，未知错误

**重试命令**:
针对一个失败主机生成、可由核心用户直接执行或调整的单主机命令。
_Avoid_: 重新执行全部

**平台功能平等**:
Linux、macOS 和 Windows 最终提供相同正式能力与用户语义，但交付时间可以不同。
_Avoid_: 同时交付，完全相同实现

## Relationships

- 一个 **核心用户** 独立管理约 5～100 台 **主机**
- 一台新添加的 **主机** 可以先作为 **主机草录** 存在，之后再显式验证
- 一个 **托管密钥** 可以绑定到多个 **主机**
- 一个 **主密码会话** 只属于一个当前运行的 sshm 进程
- 一个 **临时密码** 只属于一次当前连接尝试
- 一个 **目标集合** 包含一个或多个 **主机**
- 一台 **主机** 可以拥有多个 **标签**
- 一台 **主机** 可以处于 **收藏** 状态，并拥有一个 **最近使用** 时间
- 一个 **批量操作** 作用于一个 **目标集合**
- 一个 **连接旅程** 最终连接到一台 **主机**
- 一个 **主机信任** 对应一台已确认身份的 **主机**
- 一个危险的 **批量操作** 需要一次 **显式批准**
- 一个远程写入、命令执行或多主机操作需要一次 **执行确认**
- 每个受支持的 SSH 操作都必须是 sshm 的 **原生能力**
- **传输加速** 不可用时，文件传输仍由 **原生能力** 完成
- 一次文件传输为每台目标 **主机** 产生一个 **传输结果**
- 替换现有内容需要一次 **覆盖批准**
- 一台 **主机** 可以通过一台 **跳板机** 建立连接
- 一次 **私钥导入** 产生一个 **托管密钥**
- 一次 **本地端口转发** 依赖一台已建立 SSH 连接的 **主机**
- 一个 **核心用户** 拥有一个完整的 **数据目录**
- 一个 **连接旅程** 可以从 **工作台** 或 **别名直连** 开始
- 一个 **批量操作** 为目标集合中的每台 **主机** 产生一个 **逐主机结果**
- 一个失败的 **逐主机结果** 包含一个 **失败阶段** 和一个 **重试命令**
- 一个批量命令使用 **详细结果模式** 或 **简洁状态模式** 展示逐主机结果
- **简洁状态模式** 将隐藏的完整结果写入一个 **操作日志**

## Example dialogue

> **产品经理:** “核心用户为什么不直接维护 OpenSSH 配置？”
> **领域专家:** “因为他需要独立管理几十台主机，希望更快地查找、连接和批量操作，但不需要团队协作或企业堡垒机。”

## Flagged ambiguities

- “用户”可能指本地使用 sshm 的人，也可能指远程 SSH 用户名；已将前者定义为 **核心用户**，远程登录身份继续称为“SSH 用户名”。
- “小团队运维人员”曾被列为核心用户；已确认产品只面向个人使用，不包含团队场景。
- “支持 SSH 参数”曾可能表示透传系统 OpenSSH 参数；已确认只支持 sshm 自身定义的 **原生能力**。
- “纯 Go 原生实现”不排斥文件传输使用可选的 **传输加速**；核心 SSH 行为和回退能力仍由 sshm 原生提供。
- “支持普通文件私钥”指执行一次 **私钥导入**，不指长期保存或依赖文件路径。
- “同步”和“备份”由核心用户通过复制完整 **数据目录** 完成，不是 sshm 的产品能力。
- “交互模式”统一称为 **工作台**；它是轻量入口，不是复杂全屏 TUI。
- “三平台完全平等”指 **平台功能平等**，不要求所有新能力在三个操作系统同时交付。
- “分组”已被多选 **标签** 取代，不再作为产品概念。
- **目标集合** 只描述一次操作选中的主机，不会被命名或持久保存。
- “解锁一次”只指当前进程中的 **主密码会话**，不跨 CLI 命令或工作台进程共享。
- “添加主机”不包含远端连接或认证验证；未验证的配置称为 **主机草录**。
- **临时密码** 成功连接不会自动改变主机认证策略或部署密钥。
- `--yes` 只跳过一次 **执行确认**，不跳过主机信任、主密码或其他安全控制。
- **操作日志** 是个人排障数据，不是企业审计能力。
- **操作日志** 默认保留 30 天，不额外加密，并由当前用户专属文件权限保护。
- 批量命令默认使用 **详细结果模式**；只有核心用户显式使用 `--quiet` 时才进入 **简洁状态模式**。
